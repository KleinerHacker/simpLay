# j-print - Implementation

The `j-print` module sends the output of a running simulation from the `engine`
core to a printer.

## Add the dependency

```kotlin
dependencies {
    implementation("org.pcsoft.framework:simplay-engine:<version>")
    implementation("org.pcsoft.framework:simplay-j-print:<version>")
}
```

## Entry points

!!! note

    The public API of the printing integration is not available yet. This page
    will document the entry points and configuration as soon as the `j-print` API
    is released.
